import 'package:flutter/material.dart';

class TopMesPage extends StatelessWidget {
  const TopMesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
        child: Text('Top Mes Page'),
      ),

    );
  }
}