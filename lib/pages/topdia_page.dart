import 'package:flutter/material.dart';

class TopDiaPage extends StatelessWidget {
  const TopDiaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
        child: Text('Top Dia Page'),
      ),
    );
  }
}
